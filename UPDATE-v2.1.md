# TDnetレーダー v2.1

画面・サービスワーカーとCloudflare用更新コード。既存の通知判定ルールはv2から変更していません。

## 機能

- Cloudflare更新後の通知本体は該当TDnet原文へ直接移動。
- 通知の「かんたん説明」から詳細ページへ移動。
- 複合材料をそれぞれ説明し、説明取得の失敗時も原文リンクを残す。
- タイトルを言い換える無料のルールベース説明。PDF本文の要約・翻訳は未実装。
- 端末内通知の起動先とキャッシュ処理を修正。

## Cloudflare側の反映が別途必要

このリポジトリへの保存だけではCloudflareの通知処理は更新されません。

1. Cloudflareの `tdnet-monitor` を開き、現在のコードを保存。
2. `cloudflare/worker.js` の全文で現在のコードを置き換えてデプロイ。
3. 環境変数、Secret、KV、2分Cronは既存のものを維持。
4. WorkerのJSONで `version: "2.1"` を確認。
5. 更新後の新しい通知で、本文タップ→原文、説明ボタン→該当会社の説明を確認。

Workerの変更元は2026年9月7日の保存版v2。本番Workerのコードはアクセス制限により未照合です。9月7日以降に手動変更した場合は差分を確認してください。
古い通知のURLは変わりません。説明の保存は35日、TDnet原文の公開期限はTDnet側に依存します。

## テスト

Node.js 24で `node --test tests/tdnet.test.cjs`。
16テスト、327タイトルの分類回帰、通知送信JSON、KV保存失敗、詳細ページの通信失敗、サービスワーカーの遷移・キャッシュを模擬検証。
実通知のAndroid実機確認と実ブラウザのレイアウト検証は未完了です。
`tests/fixtures/v2/` は回帰テスト用の旧コードで、本番へ配置するコードではありません。
